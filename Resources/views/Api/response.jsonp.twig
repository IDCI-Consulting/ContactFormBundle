{{ app.request.get('callback') }}([
{% spaceless %}
  {% for response in responses %}
  {
    "code" : "{{ response.code }}",
    "message" : "{{ response.message }}"
  }{% if not loop.last %},{% endif %}
  {% endfor %}
{% endspaceless %}
])
